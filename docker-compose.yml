version: "3.8"
services:
  datatitan_site:
    build: ./
    ports:
      - "8080:8000"
    networks:
      - frontend
      - backend

  memcached:
    image: library/memcached:latest
  db:
    image: library/mysql:latest
    restart: always


volumes:
  db-data:

secrets:
  postgres_password:
    external: true

networks:
  frontend:
    driver:

  backend:
    driver: